# Trace Race Production 0.1.3 - Parser Fixed

Built from the confirmed-working Engine v0.5 main.gd.

Changes are intentionally minimal:
- Explicit types on every new menu variable
- Explicit event casts
- ASCII-only menu strings
- No image preload or external UI resource loading
- Race engine remains the v0.5 baseline

Test: Run -> PLAY -> draw a complete lap -> RACE.
